[options]
admin_passwd = mysupersecretpassword
db_host = localhost
db_port = 5432
db_user = root
db_password = admin
addons_path = addons
xmlrpc_port = 8019

# logfile = odoo.log
# data_dir = /opt/odoo17/.local/share/Odoo

# download branch:
# git clone https://www.github.com/odoo/odoo.git -b 17.0 --depth 1

# Start Odoo normally
# python odoo-bin -c odoo.conf

# Run this command only when needed for updates or initialization
# python odoo-bin -c odoo.conf -d eventslight -i base

# generate a basic structure for your app
# python odoo-bin scaffold <module_name> custom_addons/
